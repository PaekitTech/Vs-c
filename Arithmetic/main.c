#include <stdio.h>

int main() {

	// arithmetic operators + - * / % ++ --
	// can use int float

	int x = 10;
	int y = 2;
	int z = 0;

	//z = x + y;
	//z = x - y;
	//z = x * y;
	//z = x / y;
	z = x % 2;
    //printf("%d", z);

	// x++ increment +1
	// x-- decrement -1
    //x++;
    //x--;

	// augmented assignment operators
	//x+=3; x+3
	//x-=3; x-3
	//x*=3; x*3
	//x/=3; x/3

	printf("%d", z);

	return 0;
}