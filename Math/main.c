#include <stdio.h>
#include <math.h>

int main() {

    //int x = 3;
    float x = 45;

    //x = sqrt(3); สแควรูท
    //x = pow(x, 3); ยกกำลัง
    //x = round(x); ปัดเศษ <.50 = ปัดลง , >=.50 = ปัดขึ้น
    //x = ceil(x); ปัดขึ้นอย่างเดียว
    //x = floor(x); ปัดลงอย่างเดียว
    //x = abs(x); เปลี่ยนจากค่าลบไปบวก ใช้กับ int
    //x = log(x); หาค่าlog
    //x = sin(x); หาค่าsin
    //x = cos(x); หาค่าcos
    //x = tan(x); หาค่าtan

    //when use ## int
    //printf("%d", x);

    //when use ## float
    printf("%f", x);

    return 0;
}