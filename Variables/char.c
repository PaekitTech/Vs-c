#include <stdio.h>

int main() {

    // char use for if it not number

    char grade = 'B';
    char symbol = '!';
    char charactor = 'X';

    // if use char use %c

    printf("My grade is %c\n", grade);
    printf("My favorite symbol is %c\n", symbol);
    printf("My favorite charactor is %c", charactor);

    return 0;
}