#include <stdio.h>

int main() {

    // char use for if it not number

    char name[] = "Pae";
    char symbol[] = "!?";
    char charactor[] = "Baki";

    // if use char use %c

    printf("My grade is %s\n", name);
    printf("My favorite symbol is %s\n", symbol);
    printf("My favorite charactor is %s", charactor);

    return 0;
}