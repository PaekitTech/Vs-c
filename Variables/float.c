#include <stdio.h>

int main() {

    //float can use 0.1 to show in output

    float kilo = 1.5;
    float weight = 86.5;
    float temperature = -12.2;

    // if use float then use %f to make float inside %f

    printf("I just buy pork %fkg\n",kilo);
    printf("I am weight %fkg\n",weight);
    printf("It very cool outside i think it %f'c",temperature);

    return 0;
}